# Zathura

Minimal document viewer for the web

## Quick Start

```bash
pnpm install
pnpm dev:web
```

## Scripts

- `pnpm dev` - dev all apps
- `pnpm dev:web` - dev web app only
- `pnpm build` - build all apps
- `pnpm check-types` - typecheck
- `pnpm check` - lint and format
